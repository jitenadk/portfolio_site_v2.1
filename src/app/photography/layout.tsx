export const metadata = {
  title: 'Photography | jitendotexe',
  description: 'A collection of moments captured through my lens',
}

export default function PhotographyLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
