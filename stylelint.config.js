module.exports = {
  extends: ['stylelint-config-tailwindcss'],
  rules: {
    // Add any specific rule overrides here if needed
    // e.g., "at-rule-no-unknown": [true, { "ignoreAtRules": ["tailwind", "apply", "layer", "config"] }]
    // The config-tailwindcss preset should handle this, but keep it in mind if issues persist.
  },
};
